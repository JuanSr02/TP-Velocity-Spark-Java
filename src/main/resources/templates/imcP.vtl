<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora de IMC</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Estilo personalizado */
        .formulario {
            max-width: 400px;
            margin: 0 auto;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Calculadora de Índice de Masa Corporal (IMC)</h1>
        <form class="formulario" action="/IMC" method="get">
            <div class="mb-3">
                <label for="peso" class="form-label">Peso (kg):</label>
                <input type="number" class="form-control" id="peso" name="peso" required>
            </div>
            <div class="mb-3">
                <label for="altura" class="form-label">Altura (cm):</label>
                <input type="number" class="form-control" id="altura" name="altura" required>
            </div>
            <button type="submit" class="btn btn-primary">Calcular IMC</button>
        </form>
        <div id="resultados-imc" class="mt-4">
            <h2 class="text-center">Resultados del Índice de Masa Corporal (IMC)</h2>
            <p class="text-center">Su IMC es: $imc <span id="imc"></span></p>
            <p class="text-center">Valoración: $valoracion <span id="valoracion"></span></p>
        </div>
    </div>

    <!-- Bootstrap JavaScript y cualquier otro script necesario aquí -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

